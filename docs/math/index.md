# 수학 기초

## 이 장의 목표

- 함수와 로그, 지수를 모델 스케일 관점으로 읽는다.
- 벡터와 행렬을 "데이터와 파라미터를 담는 그릇"으로 이해한다.
- 미분을 변화율이 아니라 "손실을 줄이는 방향 신호"로 이해한다.
- 확률을 불확실성과 예측의 언어로 받아들인다.

## 왜 이 섹션이 특히 중요한가

이 책에서 수학 섹션은 선택 과목이 아니라 기반 공사다. 여기서 헷갈리면 이후 PyTorch, Transformer, GPT-3, RLHF를 전부 "용어 암기"로 배우게 된다.

## 이 장에서 꼭 가져갈 한 문장

수학은 따로 존재하는 과목이 아니라, 이후 모든 모델과 학습 루프를 읽기 위한 언어다.

## 권장 학습 순서

<MermaidDiagram
  :code="`flowchart TD
  A[&quot;함수, 로그, 지수&quot;] --> B[&quot;벡터와 행렬&quot;]
  B --> C[&quot;미분과 gradient&quot;]
  C --> D[&quot;확률과 softmax&quot;]
  D --> E[&quot;PyTorch training loop&quot;]`"
/>

## 수학 집중 TODO

- [ ] 함수 그래프와 loss curve를 연결하는 설명 추가
- [ ] 로그와 지수를 cross-entropy, softmax와 연결
- [ ] 행렬곱을 임베딩과 선형층 사례로 설명
- [ ] chain rule을 backpropagation 그림과 연결
- [ ] 확률 분포, 기대값, 분산을 샘플링과 평가 관점으로 연결
- [ ] 각 하위 페이지 끝에 연습 문제 3개 이상 추가

## 수학 서브모듈

| 서브모듈 | 핵심 질문 | 페이지 |
| --- | --- | --- |
| 함수, 로그, 지수 | 모델에서 값이 왜 비선형으로 움직이는가 | [함수, 로그, 지수](/math/functions-growth) |
| 벡터와 행렬 | 데이터를 어떻게 묶고 변환하는가 | [벡터와 행렬](/math/vectors-matrices) |
| 미분과 gradient | 손실을 어떻게 줄이는가 | [미분과 gradient](/math/gradients) |
| 확률과 softmax | 모델 출력은 왜 분포인가 | [확률과 softmax](/math/probability) |

## 최소 수학 실라버스

### 1. 함수, 로그, 지수

신경망에서는 값이 선형으로만 움직이지 않는다. softmax, cross-entropy, learning rate decay처럼 로그와 지수는 계속 등장한다.

### 2. 벡터와 행렬

- 벡터: 한 샘플의 특징 묶음
- 행렬: 여러 샘플 또는 가중치 묶음
- 행렬곱: 입력 특징과 가중치를 섞어 새로운 표현을 만드는 연산

### 3. 미분과 gradient

미분은 "조금 바꿨을 때 손실이 얼마나 달라지는가"를 측정한다. gradient descent는 이 정보를 이용해 파라미터를 반대 방향으로 움직이는 절차다.

```text
new_weight = old_weight - learning_rate * gradient
```

### 4. 확률과 기대값

모델은 정답을 확실히 아는 기계가 아니라, 가능한 답의 분포를 추정하는 기계다. 기대값은 평균적인 결과를, 분산은 흔들림의 정도를 보여준다.

## 꼭 연결해야 하는 것

| 수학 개념 | 모델에서의 역할 |
| --- | --- |
| 벡터 | 토큰, 임베딩, 특성 표현 |
| 행렬곱 | 선형 변환과 attention 계산 |
| 미분 | 역전파와 파라미터 업데이트 |
| 확률 | 로짓, softmax, 샘플링 |

## 논문 읽기 관문

수학 장을 끝낼 때 목표는 아래 표현을 자연스럽게 읽는 것이다.

| 표현 | 읽는 법 |
| --- | --- |
| `XW` | 입력 표현 `X`를 가중치 `W`로 새 공간에 투영한다 |
| `QK^T` | 토큰 사이 유사도 점수 표를 만든다 |
| `softmax(z)` | 점수 `z`를 확률 분포처럼 읽게 만든다 |
| `L = -log p(y)` | 정답 확률이 낮을수록 손실이 커진다 |
| `grad_theta L` | 파라미터 `theta`를 어떻게 움직여야 loss가 줄어드는지 알려준다 |

## 표기법 생존 가이드

논문에서 가장 자주 보이는 기호는 아래 정도다.

- 소문자 `x`, `h`, `y`: 보통 벡터 한 개 또는 스칼라
- 대문자 `X`, `W`, `Q`, `K`, `V`: 보통 행렬 또는 여러 벡터 묶음
- `p(.)`: 확률 분포 또는 확률 함수
- `L`: loss
- `theta`: 학습되는 전체 파라미터

이 기호들은 이름보다 역할로 읽는 습관이 중요하다. `W`를 보면 "가중치", `p`를 보면 "분포", `L`을 보면 "최소화 대상"이라고 바로 떠올라야 한다.

## 이 장을 끝내면 할 수 있어야 하는 것

- 논문 식에서 어떤 항이 데이터이고 어떤 항이 파라미터인지 구분한다.
- `shape`를 말로 설명한다.
- `softmax`, `log`, `expectation`, `gradient`가 왜 등장하는지 설명한다.
- 수학 기호를 PyTorch 코드의 텐서 연산으로 대응시킨다.

## 연습

1. 벡터와 행렬의 차이를 자신의 말로 설명해본다.
2. 손실 함수가 크다는 말이 무엇을 의미하는지 적어본다.
3. softmax가 왜 확률처럼 보이는지 정리해본다.

## 다음 장으로 어떻게 연결되는가

수학을 모델 언어로 읽기 시작했다면, 이제 그 언어를 실제 텐서와 학습 루프로 옮기는 PyTorch 장으로 넘어갈 수 있다.

다음 장: [Python과 PyTorch](/python-pytorch/)
